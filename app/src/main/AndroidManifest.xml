<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Changia"
        tools:targetApi="31">

        <!-- ===== LAUNCHER ACTIVITY (ONLY ONE) ===== -->
        <!-- SPLASH ACTIVITY (LAUNCHER) - KEEP ONLY THIS ONE WITH LAUNCHER INTENT -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Changia.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== AUTH ACTIVITIES ===== -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />  <!-- REMOVED LAUNCHER INTENT -->

        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- ADD THIS - MISSING ACTIVITY -->
        <activity
            android:name=".CompleteProfileActivity"
            android:exported="false" />

        <!-- ===== MAIN ACTIVITY ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <!-- ===== PROFILE ===== -->
        <activity
            android:name=".ProfileActivity"
            android:exported="true" />

        <!-- ===== WALLET MODULE (ADD THESE - MISSING) ===== -->
        <activity
            android:name=".AddMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".AddMoneyActivity_RealMpesa"
            android:exported="false" />
        <activity
            android:name=".SendMoneyActivity"
            android:exported="false" />
        <activity
            android:name=".WithdrawActivity"
            android:exported="false" />
        <activity
            android:name=".TransactionHistoryActivity"
            android:exported="false" />
        <!--<activity
            android:name=".TransactionDetailsActivity"
            android:exported="false" /> -->

        <!-- ===== GROUPS MODULE ===== -->
        <activity
            android:name=".CreateGroupActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".GroupDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".MemberListActivity"
            android:exported="false" />

        <!-- ===== CONTRIBUTIONS MODULE ===== -->
        <activity
            android:name=".ContributeActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <!-- ===== LOCKING MODULE ===== -->
        <activity
            android:name=".LockRequestActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".VoteActivity"
            android:exported="false" />

        <!-- ===== DASHBOARD ===== -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

    </application>

</manifest>